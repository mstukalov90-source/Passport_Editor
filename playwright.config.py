"""Minimal Playwright config for pytest-playwright (base URL set per test via live_server)."""

from __future__ import annotations

# pytest-playwright reads this module when present; tests use live_server URLs directly.
